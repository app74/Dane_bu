[project]
name = "danove-platby-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115,<1", "uvicorn[standard]>=0.30,<1", "sqlalchemy>=2,<3", "alembic>=1.13,<2", "pydantic>=2,<3", "python-multipart>=0.0.20,<1"]
[project.optional-dependencies]
dev = ["pytest>=8,<9", "httpx>=0.27,<0.28", "ruff>=0.6,<1"]
[tool.pytest.ini_options]
pythonpath = ["."]
[tool.ruff]
line-length = 100
target-version = "py312"
[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.setuptools.packages.find]
include = ["app*"]
